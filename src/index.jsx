//import react and append app to dom at element #cow;
import React from 'react';
import ReactDOM from 'react-dom';
import App from '.App.js';

ReactDOM.render(<App />, document.getElementById('cow'));